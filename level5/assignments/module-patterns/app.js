const { sum, subtract } = require("./math");
const { multiply, setFactor } = require("./math2");
const User = require("./user");

// console.log(sum(5, 5));
// console.log(multiply(2));

// setFactor(20);

// console.log(multiply(10));

console.log(new User("Steve", 10));
