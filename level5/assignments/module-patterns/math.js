function sum(a, b) {
  return a + b;
}

function subtract(a, b) {
  return a - b;
}

module.exports = { sum, subtract };

// also can use --> module.exports.sum = function sum(a, b) = { return a +  b}
